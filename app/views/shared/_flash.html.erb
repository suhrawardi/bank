<% unless notice.blank? and alert.blank? %>
  <div class="row">
    <% unless notice.blank? %>
      <div class="alert alert-info success"><%= notice %></div>
    <% end %>
    <% unless alert.blank? %>
      <div class="alert alert-info danger"><%= alert %></div>
    <% end %>
  </div>
<% end %>
